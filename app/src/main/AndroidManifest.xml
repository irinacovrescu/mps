<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.testproject">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".AdminMenu.ParticipantsActivity"></activity>
        <activity
            android:name=".AdminMenu.ParticipantsSetUpActivity"
            android:label="@string/title_activity_contestants"
            android:windowSoftInputMode="adjustPan"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".AdminMenu.ContestSetUpActivity"
            android:label="@string/title_activity_contest_set_up"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".AdminMenu.JuryActivity"
            android:label="@string/title_activity_jury"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".LeaderBoardActivity"
            android:label="LeaderBoardActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".AdminMenu.AdminMenuActivity"
            android:label="@string/title_activity_admin_menu"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".AuthActivity" />
        <activity android:name=".GradingActivity" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>